namespace Domain.Enum;

//NOTE: All code types should be present here and never be hardcoded anywhere
public enum ECodeType
{
    TITLE,
    USER_TYPE,
    VENDOR_CATEGORY,
    CATALOG_CATEGORY,
    UNIT_OF_MEASURE,
    DELIVERY_LOCATION,
    CURRENCY,
}
