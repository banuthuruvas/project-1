namespace Domain.Enum;

/// <summary>
/// Distinguishes access functions that protect screens from those that protect APIs.
/// </summary>
public enum EAccessFunctionType
{
    Screen = 1,
    Api = 2
}
